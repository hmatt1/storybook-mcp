{
  "name": "storybook-mcp-server",
  "version": "1.0.0",
  "description": "A lightweight server that enables AI assistants to visually analyze Storybook UI components",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc || exit 0",
    "start": "node dist/index.js",
    "dev": "ts-node --esm src/index.ts",
    "docs": "jsdoc -c jsdoc.json",
    "docs:dev": "npm run docs && serve docs",
    "lint": "eslint src/**/*.ts",
    "test:windows": "npm run build && .\\tests\\run-tests.bat",
    "test:linux": "npm run build && bash ./tests/run-tests.sh",
    "test": "npm run build && node -e \"process.platform === 'win32' ? require('child_process').execSync('tests\\\\run-tests.bat', {stdio: 'inherit'}) : require('child_process').execSync('chmod +x tests/run-tests.sh && tests/run-tests.sh', {stdio: 'inherit'})\"",
    "test:integration": "npm run build && node -e \"process.platform === 'win32' ? require('child_process').execSync('tests\\\\run-tests.bat', {stdio: 'inherit'}) : require('child_process').execSync('chmod +x tests/run-tests.sh && tests/run-tests.sh', {stdio: 'inherit'})\"",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "storybook",
    "ai",
    "mcp",
    "ui",
    "components"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.7.0",
    "express": "^5.0.1",
    "playwright": "^1.41.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^20.11.0",
    "docdash": "^2.0.1",
    "eslint": "^8.56.0",
    "jsdoc": "^4.0.2",
    "serve": "^14.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}